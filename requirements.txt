# CHAPPiE full installation, Python 3.11+
# Existing `pip install -r requirements.txt` behavior stays complete.
-r requirements/runtime.txt
-r requirements/providers-local.txt
-r requirements/training.txt
